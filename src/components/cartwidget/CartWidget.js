import React from 'react';

const CartWidget = () => {
    return (
        <span role="img" aria-label="cart" style={{ fontSize: '24px', marginLeft: '20px' }}>
            🛒
        </span>
    );
}

export default CartWidget;